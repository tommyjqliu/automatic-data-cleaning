{
  "name": "automatic-data-cleaning",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:client": "cd client && npm install",
    "install:server": "cd server && pip install -r requirements.txt",
    "install": "npm run install:client && npm run install:server",
    "dev": "cd client && npm run dev"
  }
}
